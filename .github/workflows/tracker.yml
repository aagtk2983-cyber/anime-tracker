name: Daily Anime Tracker

"on":
  schedule:
    - cron: "0 12 * * *"   # 21:00 JST = 12:00 UTC
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  track:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Japanese font (グラフの日本語表示用)
        run: sudo apt-get update && sudo apt-get install -y fonts-noto-cjk

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Install Playwright browser
        run: python -m playwright install --with-deps chromium

      - name: Run tracker (登録タイトル全件のMAL・dアニメ・ABEMA情報を取得)
        env:
          # ABEMAは日本国外IPから中身が取得できないため、日本国内プロキシを
          # 使う場合はリポジトリのSecretsに登録して渡す。未設定でも動く
          # (その場合ABEMA部分は地域制限で失敗しログに出るだけ)。
          ABEMA_PROXY_SERVER: ${{ secrets.ABEMA_PROXY_SERVER }}
          ABEMA_PROXY_USERNAME: ${{ secrets.ABEMA_PROXY_USERNAME }}
          ABEMA_PROXY_PASSWORD: ${{ secrets.ABEMA_PROXY_PASSWORD }}
        run: python daily_tracker.py

      - name: Plot trends
        run: python plot_trend.py

      - name: Upload debug files (調査用)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-files
          path: data/debug_*
          if-no-files-found: ignore

      - name: Commit and push updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/ trends/
          git diff --cached --quiet && echo "No changes to commit" || git commit -m "Daily Update $(date -u +'%Y-%m-%d')"
          git push
